import { StyleLinksDirective } from './style-links.directive';

describe('StyleLinksDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleLinksDirective();
    expect(directive).toBeTruthy();
  });
});
