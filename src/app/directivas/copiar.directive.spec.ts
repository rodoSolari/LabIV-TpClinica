import { CopiarDirective } from './copiar.directive';

describe('CopiarDirective', () => {
  it('should create an instance', () => {
    const directive = new CopiarDirective();
    expect(directive).toBeTruthy();
  });
});
